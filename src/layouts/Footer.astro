---
import Container from "../components/shared/Container.astro";
import CtaButton from "../components/shared/CtaButton.astro";
import GradiantText from "../components/shared/GradiantText.astro";
import Paragraph from "../components/shared/Paragraph.astro";

export interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<section id="cta" class=`pb-5 ${className}`>
  <Container>
    <div class="w-full relative lg:pt-16 pt-20 pb-8 md:py-10 px-6 md:px-8 rounded-2xl bg-gradient-to-br from-slate-100 to-zinc-200 dark:from-slate-800 dark:to-zinc-800">
      <div class="mx-auto text-center max-w-xl md:max-w-2xl relative">
        <h1 class="text-3xl/tight sm:text-4xl/tight md:text-5xl/tight font-bold text-heading-1">
          Estás listo para
          <GradiantText> comenzar </GradiantText>
          a  
          <GradiantText> disfrutar </GradiantText>
          del tiro con arco
        </h1>
          <Paragraph className="pt-10 font-semibold">
          Tanto si deseas iniciarte en el tiro con arco, perfeccionarte o resetear tus capacidades, con mi ayuda
          podrás alcanzar tus metas de una manera más eficiente, organizada y motivada.
          </Paragraph>
        <Paragraph className="pt-10 font-semibold">
          Ponte en contacto conmigo y te explico 
        </Paragraph>
        <div class="mx-auto max-w-md sm:max-w-xl pt-10">
          <CtaButton />
        </div>
      </div>     
    </div>
  </Container>
</section>
