---
import { Image } from "astro:assets";
import Button from "../shared/Button.astro";
import Container from "../shared/Container.astro";
import Paragraph from "../shared/Paragraph.astro";
import ByNumber from "./ByNumber.astro";
import myHeroImage3 from "../../assets/images/img_66890.jpg";
import CtaButton from "../shared/CtaButton.astro";
import ByResult from "./ByResult.astro";
import GradiantText from "../shared/GradiantText.astro";

export interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<section class=`relative pt-32 lg:pt-36 ${className}`>
  <Container className={"flex flex-col lg:flex-row gap-10 lg:gap-12"}>
    <div class="relative flex flex-col items-center text-center lg:text-left lg:py-7 xl:py-8
        lg:items-start lg:max-w-none max-w-3xl mx-auto lg:mx-0 lg:flex-1 lg:w-1/2">
      <h1 class="text-2xl/tight sm:text-4xl/tight md:text-4xl/tight xl:text-5xl/tight font-bold text-heading-1">
        Quieres mejorar
        <GradiantText>tu habilidad y tú técnica<br /> en </GradiantText>
        tiro con arco
      </h1>
      <Paragraph className="mt-8 ">
         Tú viaje hacia una práctica satisfactoria llena de exitos y superación personal
         comienza aqui.
      </Paragraph>

      <div class="mt-16 w-full flex flex-col">
        <div class="lg:mx-auto mb-5">
          <CtaButton />
        </div>
        <div class="mx-auto">
          <ByResult />
        </div>
      </div>
    </div>
    <div class="flex lg:w-1/2 lg:h-auto relative lg:max-w-none lg:mx-0 mx-auto max-w-3xl">
      <Image src={myHeroImage3}
        alt="Record de España Aire libre 2023"
        class="my-class"
        width="500"
        height="500"
        class=" lg:absolute rounded-3xl object-cover lg:max-h-none max-h-96"
      />
    </div>
  </Container>
  <ByNumber/>
</section>
